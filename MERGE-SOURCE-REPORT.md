# Daxora Ground Control Ultimate Merge Source Report

Primary baseline: latest uploaded `hsm-scheduler(8).zip`, commit 40892a8.
Feature source: latest uploaded `hsm-scheduler-teamfeepay.zip`.

Merge policy:
- Primary baseline remains Ground Control.
- TeamFeePay branch overlays its newer/common files and contributes files absent from the primary baseline.
- Git/build/cache artifacts are excluded from the merged application payload.
- `dist/` is intentionally excluded and must be regenerated by the production build.
- `.release-evidence/` from the feature archive is excluded as historical generated evidence.
- No Supabase migration is removed; unique migrations from both branches are retained.

This is a consolidated technical baseline. Product simplification decisions (Mission Control, Organisation Command, Coach Hub, Annual Planner, League Manager, etc.) are deliberately deferred until after validation.
